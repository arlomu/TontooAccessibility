[package]
name = "tontoo-accessibility"
version = "0.1.0"
edition = "2021"
description = "Rust i18n library for embedding in other projects"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
once_cell = "1.19"
